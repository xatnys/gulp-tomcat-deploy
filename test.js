'use strict';

var assert = require('assert'),
    es = require('event-stream'),
    gutil = require('gulp-util'),
    deploy = require('./index');

describe('gulp-tomcat-deploy', function () {
  // untested
});
